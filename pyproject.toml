[project]
name = "ai-image-generation"
version = "0.1.0"
description = "Domain models for AI image generation prompts (camera, character, and shoot settings)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ai-image-generation = "ai_image_generation.cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"
